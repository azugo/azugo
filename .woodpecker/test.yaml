when:
  branch:
    - main
  event:
    - pull_request

matrix:
  GO_VERSION:
    - 1.19
    - 1.20

pipeline:
  - name: test
    image: golang:${GO_VERSION}-alpine
    pull: true
    commands:
      - go test -race ./...
